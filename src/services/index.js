const UserService = require('./user.service');
const PetService = require('./pet.service');
const AdoptService = require('./adopt.service');

module.exports = {
  UserService,
  PetService,
  AdoptService,
};
