const AuthController = require('./auth.controller');
const PetController = require('./pet.controller');
const AdoptController = require('./adopt.controller');
const UserController = require('./user.controller');

module.exports = {
  AuthController,
  PetController,
  AdoptController,
  UserController,
};
